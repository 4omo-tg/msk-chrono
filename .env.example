# ============================================
# Moscow Chrono Walker - Конфигурация
# ============================================
# Скопируйте этот файл в .env и заполните значения
# cp .env.example .env
# ============================================

# -------------------- ОБЯЗАТЕЛЬНЫЕ --------------------

# Telegram Bot (получить у @BotFather в Telegram)
# 1. Напишите /newbot в @BotFather
# 2. Выберите имя и username для бота
# 3. Скопируйте токен сюда
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_BOT_USERNAME=your_bot_username

# Секретный ключ для JWT токенов (сгенерируйте случайную строку)
# Можно сгенерировать командой: openssl rand -hex 32
SECRET_KEY=change_me_to_random_string_32_chars_min

# -------------------- ОПЦИОНАЛЬНЫЕ --------------------

# AI API для верификации фото (по умолчанию бесплатный прокси)
AI_API_BASE_URL=https://ai-proxxy.exe.xyz/api
AI_API_KEY=
AI_MODEL=qwen3-vl-plus

# URL сайта (для ссылок в Telegram боте)
# Измените если деплоите на свой домен
SITE_URL=https://test-serv.exe.xyz:8000

# -------------------- БАЗА ДАННЫХ (Docker) --------------------
# Эти настройки используются docker-compose автоматически
# Не меняйте если используете docker-compose

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=moscow_chrono

# Для локальной разработки без Docker:
# DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/moscow_chrono
